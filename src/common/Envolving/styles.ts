import styled from "styled-components"; 

export const Envolving = styled.div` 
    background-color: ${props => props.color};
    margin: 0 auto;
    padding: 2rem;
    with: 100vw;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`;